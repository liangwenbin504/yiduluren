# 合参 BUG 审查+修复+择日迁移 报告（2026-08-19）

## 一、BUG 审查结论：3 个 BUG，全部修复并验证

| BUG | 严重度 | 症状 | 根因 | 修复 |
|---|---|---|---|---|
| A | 严重 | 线上 17 个 v6 新键（月破/旬首尾/丁神/驿马/进茹退茹等）**静默全失效**，L2 规则 N004（初传驿马+功名→凶）永不触发 | patched10 hecan 分支调 build_signals **未传 yuejiang**（v6 起必传）；验证器传了、线上没传——验证与线上不一致 | patched11 补传 `yuejiang=yuejiang` 与 `tianjiang_map` |
| B | 中 | 干上乘白虎/青龙信号语义错误：要求"干上神=初传"才查天将，Z009 等候选因此全中 0 | 早期无天将映射时的近似实现 | hecan_engine_v8 按天将映射查干上神所乘天将（无映射退回近似，兼容验证器） |
| C | 小 | "占类_谋望或求财"把"其他"算作谋望——叶池占类映射后，婚姻/行人/失物案全部误触发谋望键 | 叶池映射前全'其他'的权宜 | 改为 `zhanlei in ('求财','谋望')` |

### 修复验证
- N004 修复后在疏正功名案触发 **10 案全部 label=凶（100%）**——此前线上 0 触发。
- batch8（v8 信号）重验 37 条候选∪L2：**L2 达标 10 条全部保持 OK**，零回退。
- 全库回归（patched11 链）478 案 **0.914 零回退**（疏正 0.949 全对/汇选 0.863/指南 0.936）。

## 二、择日迁移
- **`zeri_hecan_bridge.py`**（新文件，即插即用）：封装 hecan_engine_v8 为择日入口
  `zeri_hecan_eval(ri_gan, ri_zhi, sike, sanchuan, tianjiang_map, keti, yuejiang, zetiri_type)`
  - 择日类型→占类映射：开业/交易→求财、上任/赴任→功名、安葬/动土/出行→其他
  - 返回 `{hits, narr, 警告（凶规则）, 佐证（吉规则）}`
  - **定位：警示/佐证层，不参与择日评分**（评分口径斗首/仪度/禄马贵/演禽分层已定，零回退铁律）
- 真实触发验证：庚辰日子将酉时"上任赴任"→ 命中 **Z007+N004 双凶警告**（"动而不动，升迁之信不确"正是择日语境该警示的课）。
- **接入点已就绪**：择日主服务 stock_dashboard.py（`_build_zeri_candidate` ②b 之后 4 行接入代码）写在桥文件 docstring；该文件与 api_server.py 等 9 个择日链文件均被系统级写保护（ACL 全 Allow 但删改均拒），**需在系统层面手动解锁后一次接线**。

## 三、更好的建议（择日合参的正确用法）
1. **占断规则迁移只是第一步**。合参现有 14 条多为"占功名/占病"断——择日语境语义错位。建议按同一套三层架构提炼**择日专属合参规则**：从斗首/仪度要诀/择日秘本提取"条件集合→课格吉凶"（如"龙德课+禄马到山+斗首吉星→上吉"），走 L3 候选→验证→L2 流程。
2. **择日验证池独立建**：1042 案池是占断案，不适用择日。择日实例池 = `data/zeri_case_feedback.json`（择日案例反馈落盘）+ `data/zeri_optimize_*.json`（历史优化结果）。按你"实例生成"铁律：先积累择日实例（含人工复核方向），再提规则。
3. **合参在择日只做黄牌不进球**：命中凶规则→前端标"古籍合参警示"，命中吉规则→佐证文案；绝不加减择日分。
4. **解锁清单**（建议用户系统层面手动解锁后我一次接线）：`stock_dashboard.py`（择日主服务）、`api_server.py`（占事 API import patched11）、`engine/zhanshi_duanyu.py` 等历史副本。
