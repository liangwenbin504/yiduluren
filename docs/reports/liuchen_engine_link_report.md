# generate 与 analyze 终局打通报告（632 吉凶 0.433→0.538）

> 2026-08-18 下午 · label 校正后暴露的根因修复

## 背景

`zhanshi_duanyu.generate()` 的综合评级（level）原为独立加权合成：
**占类0.6 + 三传时序0.4 + 支上0.2**。`analyze_liuchen` 的终局只输出为叙事，
**不参与吉凶评分**——此前全部深读（家宅/功名/官讼/疾病/婚姻/求财/出行/胎产）
只增强了"叙事"，632 吉凶口径从未吃到深读红利。

label 按邵公原断校正后，632 不升反降（0.443→0.433），正因 generate 对 5 案
判向与邵公原断相反（§125/132 判吉实凶、§135/157/161 判凶实吉）。

## 方案：深读终局接入评分

在 generate 合成中加入 liu 分量（analyze 终局 → 吉+2/凶-2/平0），
**权重 218 案实例反推**（`_memory/_link_search4.py`）：

- 搜索空间：w1∈[0.1,0.7]、w2∈[0.1,0.5]、w3∈[0,0.4]、w4∈[0,0.7]，步长0.1，和=1
- 约束 w1/w2 下限：避免"只信深读终局"的极端过拟合
- 验证集：壬占汇选183 + 大六壬指南78（不参与搜索，检查泛化）

结果（选平衡稳健组合，四项均有正权重）：

| 权重 | 占类 | 时序 | 支上 | 深读终局 |
|------|------|------|------|----------|
| 取值 | 0.2 | 0.2 | 0.1 | 0.5 |

搜索 Top：w4=0.6 时主集 0.677/验证 0.479；选 w4=0.5 平衡版（主 0.636/验证 0.456）。

## 成绩

| 口径 | 打通前 | 打通后 |
|------|--------|--------|
| 疏正217案 | 0.452 | **0.636** |
| 壬占汇选183案 | 0.443 | **0.481** |
| 大六壬指南78案 | 0.359 | **0.397** |
| **总体478案** | 0.433 | **0.538** |

判凶 145→205 案（疏正判凶 82→121）。

## 零回归确认

- 4库专项（analyze 口径）：36/36（婚姻3/3、求财13/13、出行13/13、胎产7/7）
- 543案走向 v2：0.340（与 HEAD 版逐项一致）
- generate 改动仅影响 level 合成，叙事/断语/应期不变

## 代码

`engine/zhanshi_duanyu.py` L434-467：新增 `_liu_score` 分量，权重改为 0.2/0.2/0.1/0.5。

## 遗留

- `engine/shuzheng_real_cases.fixed.json`（label 校正9案）已提交；主文件
  `shuzheng_real_cases.json` 受系统写保护，待解锁后合并（内容一致）。
- 报告 `liuchen_rest_deepread.md` 的"全量回归"节待解锁后同步（本文件为增量记录）。
