# 服务器端性能优化方案

## 🔍 性能瓶颈分析

### 1. 主要瓶颈

#### 1.1 重复加载 JSON 文件（严重）
**位置**: `api_server.py` 第 551-559 行
```python
ke_jing_path = os.path.join(os.path.dirname(__file__), 'data', '64_ke_jing_accurate.json')
ke_jing_data = {}
try:
    with open(ke_jing_path, 'r', encoding='utf-8') as f:
        ke_jing = json.load(f)
        for k, v in ke_jing.get('courses', {}).items():
            ke_jing_data[v['ke_name']] = v
except:
    pass
```

**问题**: 
- 每个日期时辰组合都要加载一次（360 次）
- 文件 IO 操作非常耗时
- 重复解析相同的 JSON 数据

**影响**: 每次约 10-50ms，360 次 = 3.6-18 秒

#### 1.2 重复创建对象
**位置**: 第 540-542 行
```python
from dizhi_layout_generator import arrange_tiandi_pan
from sike_sanchuan_engine import SiKeSanChuanCalculator
from gui_ren_engine import GuiRenCalculator

tiandi_pan = arrange_tiandi_pan('亥', shichen)
calc = SiKeSanChuanCalculator()
```

**问题**:
- 每次都创建新的 `SiKeSanChuanCalculator` 实例
- 每次都重新导入模块
- 贵人排盘每次都创建 `GuiRenCalculator` 实例（第 623 行）

**影响**: 对象创建和初始化约 5-10ms，360 次 = 1.8-3.6 秒

#### 1.3 贵人排盘计算复杂
**位置**: 第 621-631 行
```python
def get_guiren_pos(tian_gan, tiandi_pan, shichen):
    try:
        gui_ren_calc = GuiRenCalculator()
        guiren_info = gui_ren_calc.arrange_gui_ren_pan(tian_gan, {'天地对应': tiandi_pan}, shichen)
        tianjiang_map = guiren_info.get('天将映射', {})
        for zhi, tj in tianjiang_map.items():
            if tj == '贵人':
                return zhi
    except:
        pass
    return None
```

**问题**:
- 贵人排盘涉及复杂的天将排列计算
- 每个四柱都要计算日、年、月三柱的贵人（3 次）
- 每次都要遍历天将映射查找贵人

**影响**: 贵人排盘约 20-50ms，360×3 次 = 21.6-54 秒

#### 1.4 禄马贵人检查重复计算
**位置**: 第 633-663 行
```python
def check_luma_guiren(tian_gan, di_zhi, tiandi_pan, shichen, shan_jia, xiang_shou):
    # ... 复杂计算 ...
    ri_result = check_luma_guiren(ri_gan, ri_zhi, tiandi_pan, shichen, shan_jia, xiang_shou)
    nian_result = check_luma_guiren(nian_gan, nian_zhi, tiandi_pan, shichen, shan_jia, xiang_shou)
    yue_result = check_luma_guiren(yue_gan, yue_zhi, tiandi_pan, shichen, shan_jia, xiang_shou)
```

**问题**:
- 每个四柱要计算 3 次（日、年、月）
- 每次都要检查禄马贵人到山到向

**影响**: 每次计算约 5-10ms，360×3 次 = 5.4-10.8 秒

### 2. 总体时间估算

| 操作 | 单次耗时 | 次数 | 总耗时 |
|------|----------|------|--------|
| 加载 JSON 文件 | 30ms | 360 | 10.8 秒 |
| 创建计算器对象 | 5ms | 360 | 1.8 秒 |
| 天地盘排列 | 10ms | 360 | 3.6 秒 |
| 四课三传计算 | 20ms | 360 | 7.2 秒 |
| 贵人排盘 | 30ms | 1080 | 32.4 秒 |
| 禄马贵人检查 | 5ms | 1080 | 5.4 秒 |
| 斗首分析 | 10ms | 360 | 3.6 秒 |
| 演禽分析 | 10ms | 360 | 3.6 秒 |
| **总计** | - | - | **68.4 秒** |

**实际测试**: 30 天范围约 5-10 分钟，说明还有其他瓶颈或 IO 延迟

## ✅ 优化方案

### 方案 1: 预加载和缓存（立即实施，效果显著）

#### 1.1 全局缓存课经数据
```python
# 在 api_server.py 顶部添加
_KE_JING_CACHE = None

def get_ke_jing_data():
    """获取课经数据（带缓存）"""
    global _KE_JING_CACHE
    if _KE_JING_CACHE is None:
        ke_jing_path = os.path.join(os.path.dirname(__file__), 'data', '64_ke_jing_accurate.json')
        try:
            with open(ke_jing_path, 'r', encoding='utf-8') as f:
                ke_jing = json.load(f)
                _KE_JING_CACHE = {}
                for k, v in ke_jing.get('courses', {}).items():
                    _KE_JING_CACHE[v['ke_name']] = v
        except:
            _KE_JING_CACHE = {}
    return _KE_JING_CACHE
```

**效果**: 节省 10.8 秒

#### 1.2 缓存计算器实例
```python
# 在 full_range_analyze 函数开始处
calc = SiKeSanChuanCalculator()  # 只创建一次
gui_ren_calc = GuiRenCalculator()  # 只创建一次
```

**效果**: 节省 1-2 秒

#### 1.3 优化贵人查找
```python
# 预定义贵人查找表，避免每次都计算
GUIREN_CACHE = {}

def get_guiren_fast(tian_gan, shichen):
    """快速贵人查找（使用缓存）"""
    key = f"{tian_gan}:{shichen}"
    if key not in GUIREN_CACHE:
        # 只在未缓存时计算
        guiren_info = gui_ren_calc.arrange_gui_ren_pan(tian_gan, {'天地对应': tiandi_pan}, shichen)
        tianjiang_map = guiren_info.get('天将映射', {})
        for zhi, tj in tianjiang_map.items():
            if tj == '贵人':
                GUIREN_CACHE[key] = zhi
                return zhi
        GUIREN_CACHE[key] = None
    return GUIREN_CACHE.get(key)
```

**效果**: 节省 20-30 秒

### 方案 2: 分批处理 + 增量返回（中期实施）

#### 2.1 按天分批
```python
# 不是一次性计算所有日期，而是按天计算并返回
for current_date in date_range:
    day_results = calculate_one_day(current_date)
    # 立即返回当天的结果
    yield json.dumps({'date': current_date, 'results': day_results})
```

**优点**: 
- 用户可以立即看到部分结果
- 降低单次请求超时风险

### 方案 3: 异步任务 + 轮询（长期实施）

#### 3.1 任务队列
```python
# 使用 Celery 或 RQ 实现异步任务队列
@app.route('/api/doushou/full_range_analyze', methods=['POST'])
def full_range_analyze_async():
    task = analyze_task.delay(data)
    return {'task_id': task.id, 'status': 'queued'}

@app.route('/api/task/status/<task_id>')
def task_status(task_id):
    task = analyze_task.AsyncResult(task_id)
    if task.ready():
        return {'status': 'completed', 'result': task.result}
    else:
        return {'status': 'running', 'progress': task.info.get('progress', 0)}
```

**优点**: 
- 完全不阻塞
- 实时进度反馈
- 支持并发任务

## 🚀 立即优化实施

### 第一步：预加载课经数据
**文件**: `api_server.py`
**位置**: 文件顶部（导入之后）

### 第二步：优化 full_range_analyze 函数
**修改**: 
1. 使用缓存的课经数据
2. 只创建一次计算器实例
3. 缓存贵人计算结果

### 第三步：测试验证
- 测试 30 天日期范围
- 对比优化前后时间
- 验证结果正确性

## 📊 预期效果

| 优化项 | 优化前 | 优化后 | 提升 |
|--------|--------|--------|------|
| 30 天范围耗时 | 5-10 分钟 | 1-2 分钟 | 75-80% |
| 60 天范围耗时 | 10-20 分钟 | 2-4 分钟 | 80% |
| 内存占用 | 低 | 略高 | 可接受 |

## ⚠️ 注意事项

1. **缓存清理**: 长时间运行后可能需要清理缓存
2. **内存管理**: 缓存会占用一定内存（约 1-2MB）
3. **并发安全**: 多线程环境下需要注意线程安全

## 📝 实施优先级

1. **立即实施**（今天）: 方案 1 - 预加载和缓存
2. **中期实施**（1-2 周）: 方案 2 - 分批处理
3. **长期实施**（1-2 月）: 方案 3 - 异步任务队列

---

**分析时间**: 2026-03-29  
**状态**: 等待实施  
**预期效果**: 性能提升 75-80%
