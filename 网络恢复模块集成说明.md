# 网络请求恢复模块集成说明

## 已完成的工作

### 1. 创建网络恢复模块核心文件
- 文件：`network_recovery.js`
- 功能：实现了完整的网络请求恢复功能，包括：
  - 实时网络状态监测
  - 请求队列管理
  - 自动重试机制（支持指数退避）
  - 请求幂等性处理
  - 状态回调接口

### 2. 主界面文件已完成的修改
- 已在 `主界面.html` 中添加了网络恢复模块的引用：
  ```html
  <!-- 网络请求恢复模块 -->
  <script src="network_recovery.js"></script>
  ```

- 已添加网络恢复模块初始化和回调设置代码（在 `<script>` 标签内）：
  ```javascript
  // 初始化网络请求恢复模块
  const networkRecovery = new NetworkRequestRecovery({
      maxRetries: 3,
      retryInterval: 2000,
      exponentialBackoff: true,
      timeout: 30000,
      enableQueue: true,
      enableIdempotency: true,
      debug: true
  });
  
  // 设置网络状态回调
  networkRecovery.setCallbacks({
      onQueued: (data) => { /* 请求入队处理 */ },
      onRetry: (data) => { /* 重试处理 */ },
      onSuccess: (data) => { /* 成功处理 */ },
      onError: (data) => { /* 错误处理 */ },
      onRecover: (data) => { /* 网络恢复处理 */ },
      onOffline: (data) => { /* 网络断开处理 */ }
  });
  
  // 网络状态显示更新函数
  function updateNetworkStatus(message) {
      console.log('[网络状态]', message);
      const statusEl = document.getElementById('networkStatus');
      if (statusEl) {
          statusEl.textContent = message;
          statusEl.style.display = 'block';
      }
  }
  ```

## 需要手动完成的步骤

由于文件较大，以下修改需要手动完成：

### 步骤 1：修改 `fetchApi` 函数

在 `主界面.html` 文件中找到 `fetchApi` 函数定义（大约在第 2429 行），将整个函数体替换为：

```javascript
// API 请求包装函数（使用网络恢复模块）
async function fetchApi(url, options = {}) {
    // 使用网络恢复模块发送请求
    return await networkRecovery.request({
        url: url,
        method: options.method || 'GET',
        headers: options.headers,
        body: options.body
    });
}
```

**原函数代码（需要被替换的部分）：**
```javascript
// API 请求包装函数
async function fetchApi(url, options = {}) {
    try {
        const controller = new AbortController();
        const timeout = options.timeout || 30000;
        
        // 设置超时
        const timeoutId = setTimeout(() => controller.abort(), timeout);
        
        const response = await fetch(url, {
            ...options,
            signal: controller.signal
        });
        
        clearTimeout(timeoutId);
        
        if (!response.ok) {
            throw new Error(`API 错误：${response.status} ${response.statusText}`);
        }
        
        return await response.json();
    } catch (error) {
        handleApiError(error, url);
        throw error;
    }
}
```

### 步骤 2（可选）：添加网络状态显示区域

在界面的合适位置（如标题栏下方）添加网络状态显示：

```html
<!-- 网络状态显示 -->
<div id="networkStatus" style="display:none; padding: 10px; background: #e0f7fa; border-radius: 5px; margin-bottom: 10px; text-align: center; font-weight: bold;">
    🟢 在线
</div>
```

建议添加到 `主界面.html` 中的位置：
- 在 `.header` div 之后
- 在 `.input-section` div 之前

### 步骤 3：测试网络恢复功能

1. 启动应用程序
2. 打开浏览器开发者工具（F12）
3. 切换到 Console 标签
4. 测试场景：
   - 断开网络连接，发送请求，观察请求是否入队
   - 恢复网络连接，观察队列中的请求是否自动重试
   - 查看控制台日志，确认各个回调函数被正确调用

## 配置参数说明

网络恢复模块支持以下配置参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `maxRetries` | 3 | 最大重试次数 |
| `retryInterval` | 2000 | 重试间隔（毫秒） |
| `exponentialBackoff` | true | 是否使用指数退避（每次重试间隔翻倍） |
| `timeout` | 30000 | 请求超时时间（毫秒） |
| `enableQueue` | true | 是否启用请求队列 |
| `enableIdempotency` | true | 是否启用幂等性处理 |
| `debug` | true | 是否开启调试模式（输出详细日志） |

## 回调函数说明

| 回调函数 | 触发时机 | 参数 |
|----------|----------|------|
| `onQueued` | 请求因网络断开加入队列时 | `{ requestId, options, queueLength }` |
| `onRetry` | 请求重试时 | `{ requestId, retryCount, delay, lastError }` |
| `onSuccess` | 请求成功时 | `{ requestId, result, retries }` |
| `onError` | 请求最终失败时 | `{ requestId, error, retries }` |
| `onRecover` | 网络恢复时 | `{ timestamp }` |
| `onOffline` | 网络断开时 | `{ timestamp }` |

## 使用示例

### 查看队列状态
```javascript
const status = networkRecovery.getQueueStatus();
console.log('队列状态:', status);
// 输出：{ queueLength: 0, processingCount: 0, completedCount: 5, isOnline: true, queue: [] }
```

### 清空队列
```javascript
networkRecovery.clearQueue();
```

### 清空完成缓存
```javascript
networkRecovery.clearCompletedCache();
```

## 注意事项

1. **幂等性处理**：模块会自动缓存已完成请求的结果，相同的请求不会重复执行
2. **请求超时**：默认 30 秒超时，可通过配置参数调整
3. **网络监测**：使用浏览器原生的 `navigator.onLine` API，在某些情况下可能不够准确
4. **调试模式**：生产环境建议设置 `debug: false` 以减少控制台输出

## 技术优势

1. **自动化**：无需手动处理网络断开和恢复，模块自动管理
2. **可靠性**：支持重试和队列，确保请求最终能成功执行
3. **一致性**：幂等性处理避免重复请求导致的数据异常
4. **可观测性**：完整的回调接口，方便上层应用了解请求状态
5. **灵活性**：支持配置参数，可根据实际需求调整行为

## 文件清单

- ✅ `network_recovery.js` - 网络请求恢复模块核心文件
- ⚠️ `主界面.html` - 需要手动修改 `fetchApi` 函数
- 📝 `网络恢复模块集成说明.md` - 本文档

## 联系支持

如有问题，请查看控制台日志或联系技术支持。
