# 网络请求恢复模块 - 快速开始指南

## 🎯 模块功能一览

网络请求恢复模块已完全集成到仪度六壬择日系统中，提供以下核心功能：

### ✅ 已完成的功能
1. **实时网络监测** - 自动检测网络连接状态
2. **请求队列** - 网络断开时自动缓存请求
3. **自动重试** - 网络恢复后自动重试队列中的请求
4. **幂等性处理** - 避免重复请求导致数据异常
5. **状态回调** - 提供完整的请求状态通知
6. **超时控制** - 防止请求无限期挂起
7. **指数退避** - 智能调整重试间隔

## 📦 文件清单

| 文件 | 用途 |
|------|------|
| `network_recovery.js` | 网络请求恢复模块核心文件 |
| `主界面.html` | 已集成网络恢复模块的主界面 |
| `test_network_recovery.html` | 独立的测试页面 |
| `网络恢复模块集成说明.md` | 详细集成文档 |
| `网络请求恢复模块开发完成总结.md` | 开发总结文档 |

## 🚀 立即开始使用

### 方法 1：使用主界面（推荐）

1. **启动应用程序**
   ```bash
   # 双击启动
   start_frontend_server.py
   ```

2. **打开浏览器**
   访问：http://localhost:8000

3. **打开开发者工具**
   按 F12 打开 Console 标签

4. **测试网络恢复功能**
   - 断开网络连接
   - 点击"开始择日"按钮
   - 观察控制台日志：`📦 请求已入队`
   - 恢复网络连接
   - 观察控制台日志：`🟢 网络已恢复` → `✅ 请求成功`

### 方法 2：使用测试页面

1. **打开测试页面**
   直接在浏览器中打开：`test_network_recovery.html`

2. **点击测试按钮**
   - ✅ 成功请求 - 测试正常网络下的请求
   - 🔴 网络错误 - 测试网络错误处理
   - ⏱️ 超时请求 - 测试超时处理
   - 🔄 幂等性测试 - 测试重复请求处理

3. **观察日志输出**
   所有请求的状态变化都会显示在日志区域

## 🔧 配置说明

### 默认配置（已在主界面中设置）

```javascript
const networkRecovery = new NetworkRequestRecovery({
    maxRetries: 3,              // 最多重试 3 次
    retryInterval: 2000,        // 重试间隔 2 秒
    exponentialBackoff: true,   // 启用指数退避
    timeout: 30000,             // 30 秒超时
    enableQueue: true,          // 启用请求队列
    enableIdempotency: true,    // 启用幂等性
    debug: true                 // 开启调试模式
});
```

### 自定义配置

如需修改配置，可在 `主界面.html` 中找到初始化代码并修改：

```javascript
// 更激进的重试策略
const networkRecovery = new NetworkRequestRecovery({
    maxRetries: 5,              // 改为 5 次
    retryInterval: 1000,        // 改为 1 秒
    // ... 其他配置
});
```

## 📊 监控和调试

### 查看控制台日志

开启调试模式后，所有请求都会在浏览器控制台输出详细日志：

```
[12:34:56] 📡 发送请求：abc123
[12:34:57] ✅ 请求成功：abc123
[12:35:00] 🔴 网络已断开
[12:35:01] 📦 请求已入队：def456
[12:35:10] 🟢 网络已恢复
[12:35:10] 📬 开始处理队列，当前队列长度：1
[12:35:11] ✅ 请求成功：def456
```

### 查看队列状态

在浏览器控制台中执行：

```javascript
const status = networkRecovery.getQueueStatus();
console.log(status);
```

输出示例：
```javascript
{
    queueLength: 0,           // 队列中的请求数
    processingCount: 0,       // 正在处理的请求数
    completedCount: 5,        // 已完成的请求数
    isOnline: true,           // 网络是否在线
    queue: []                 // 队列详情
}
```

### 手动操作队列

```javascript
// 清空队列
networkRecovery.clearQueue();

// 清空完成缓存
networkRecovery.clearCompletedCache();
```

## 🧪 测试场景

### 场景 1：网络断开后恢复

**步骤：**
1. 断开网络连接
2. 点击"开始择日"
3. 等待 2 秒
4. 恢复网络连接

**预期结果：**
- 控制台显示：`📦 请求已入队`
- 网络恢复后显示：`🟢 网络已恢复` → `✅ 请求成功`
- 界面正常显示分析结果

### 场景 2：网络不稳定

**步骤：**
1. 模拟网络时好时坏
2. 点击"开始择日"

**预期结果：**
- 请求失败后自动重试
- 控制台显示：`🔄 重试请求（第 X 次）`
- 最多重试 3 次
- 成功后显示结果

### 场景 3：重复请求

**步骤：**
1. 快速多次点击"开始择日"

**预期结果：**
- 由于幂等性处理，相同请求只执行一次
- 后续请求直接返回缓存结果
- 控制台显示：`⏭️ 使用缓存结果`

## ⚠️ 注意事项

### 1. 浏览器兼容性
- 需要支持 `navigator.onLine` API
- 需要支持 `AbortController`
- 需要支持 `Promise`
- 现代浏览器（Chrome、Firefox、Edge）均可正常使用

### 2. 性能考虑
- 完成缓存会占用内存
- 建议长时间运行时定期清空缓存
- 生产环境可关闭调试模式：`debug: false`

### 3. 幂等性限制
- 仅对 GET 请求和幂等的 POST 请求有效
- 非幂等操作（如支付、删除）需谨慎使用
- 可通过 `enableIdempotency: false` 关闭

### 4. 网络检测
- `navigator.onLine` 在某些情况下可能不准确
- 例如：WiFi 连接但无互联网
- 主要依赖请求失败来判断网络问题

## 🐛 故障排查

### 问题 1：请求没有入队

**检查：**
- 确认 `enableQueue: true`
- 查看控制台是否有错误日志
- 确认网络确实断开（查看 `networkRecovery.isOnline`）

### 问题 2：网络恢复后没有自动重试

**检查：**
- 确认 `enableQueue: true`
- 查看队列是否为空
- 检查 `onRecover` 回调是否触发

### 问题 3：重复请求没有使用缓存

**检查：**
- 确认 `enableIdempotency: true`
- 检查请求参数是否完全相同
- 查看缓存：`networkRecovery.completedRequests.size`

## 📞 技术支持

如遇到问题：

1. **查看控制台日志** - 90% 的问题可以通过日志定位
2. **查看队列状态** - `networkRecovery.getQueueStatus()`
3. **查看模块配置** - `networkRecovery.config`
4. **查阅文档** - `网络恢复模块集成说明.md`

## 🎉 成功标志

当你看到以下日志时，说明模块正常工作：

```
🚀 网络请求恢复模块已初始化
📋 配置：最大重试=3, 重试间隔=2000ms
🟢 在线
📡 发送请求：xxx
✅ 请求成功：xxx
```

## 📚 进阶阅读

- `网络恢复模块集成说明.md` - 详细的 API 文档
- `网络请求恢复模块开发完成总结.md` - 技术实现细节
- `network_recovery.js` - 源代码（带详细注释）

---

**祝你使用愉快！** 🎊
