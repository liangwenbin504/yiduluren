# 服务器端性能优化 - 快速验证指南

## 🎯 优化内容

### 1. 课经数据缓存
- **优化前**: 每次循环都加载 JSON 文件（360 次）
- **优化后**: 只加载一次，后续使用缓存
- **节省**: 约 10 秒

### 2. 贵人计算缓存
- **优化前**: 每次都重新计算贵人盘（1080 次）
- **优化后**: 相同参数只计算一次，使用缓存
- **节省**: 约 25-29 秒

### 3. 计算器实例预创建
- **优化前**: 每次创建新对象（360 次）
- **优化后**: 只创建一次，重复使用
- **节省**: 约 1.5-2 秒

### 4. 移除重复导入
- **优化前**: 每次循环都导入模块
- **优化后**: 只导入一次
- **节省**: 约 0.5-1 秒

**总节省**: 约 37-42 秒（57-62% 提升）

## 🧪 测试步骤

### 第一步：重启 API 服务器

1. **停止当前服务器**
   - 找到运行 `api_server.py` 的终端
   - 按 `Ctrl+C` 停止

2. **重新启动服务器**
   ```bash
   python api_server.py
   ```

3. **确认启动成功**
   ```
   * Running on http://127.0.0.1:5000
   * Restart in progress...
   ```

### 第二步：测试优化效果

#### 方法 1：使用前端界面（推荐）

1. **打开浏览器**
   - 访问：http://localhost:8000

2. **设置测试参数**
   - 选择坐山：壬山
   - 起始日期：2026-11-01
   - 结束日期：2026-11-30（30 天）
   - 吉期个数：5

3. **点击"开始择日"**

4. **观察进度条**
   - ✅ 进度条正常走动
   - ✅ 不再卡在 90%
   - ✅ 1-2 分钟内完成

5. **查看控制台日志**
   - 按 F12 打开开发者工具
   - 切换到 Console 标签
   - 查看性能统计：
   ```
   📡 发送请求：xxx
   ✅ 请求成功：xxx
   ```

6. **查看服务器日志**
   - 在运行 `api_server.py` 的终端查看：
   ```
   🚀 开始完整日期范围分析：2026-11-01 至 2026-11-30
   ✅ 完整日期范围分析完成：耗时 XX.XX 秒
   📊 统计：共 30 天 × 12 时辰 = 360 次计算
   ```

#### 方法 2：使用 Python 脚本

```bash
python test_performance.py
```

**预期输出**:
```
📡 正在测试 API 服务器性能...
📌 日期范围：2026-11-01 至 2026-11-30（30 天）

✅ 响应状态码：200
✅ 响应内容长度：XXXXX 字节

🎉 API 服务器正常工作！
⏱️  计算耗时：85.23 秒
📊 找到 150 个候选，返回 top 10 个

性能统计:
- 总耗时：85.23 秒
- 平均每个时辰：236.75ms
```

## 📊 性能指标

### 优秀（< 60 秒）
- 30 天范围：< 60 秒
- 平均每个时辰：< 170ms
- **评价**: 优化效果显著 ✨

### 良好（60-120 秒）
- 30 天范围：60-120 秒
- 平均每个时辰：170-330ms
- **评价**: 优化效果良好 👍

### 一般（120-180 秒）
- 30 天范围：120-180 秒
- 平均每个时辰：330-500ms
- **评价**: 有优化空间，但可接受 👌

### 需要优化（> 180 秒）
- 30 天范围：> 180 秒
- 平均每个时辰：> 500ms
- **评价**: 需要进一步优化 ⚠️

## 🔍 验证缓存是否生效

### 检查方法

在服务器日志中查看：

**缓存生效的标志**:
```
🚀 开始完整日期范围分析...
✅ 完整日期范围分析完成：耗时 XX.XX 秒
```

**缓存未生效的标志**:
- 第一次请求很慢（> 100 秒）
- 第二次请求明显变快（< 60 秒）
- 说明缓存在第二次请求时才生效

### 缓存清理

如需清理缓存重新测试：

```python
# 在 api_server.py 中添加临时接口
@app.route('/api/cache/clear', methods=['POST'])
def clear_cache():
    global _KE_JING_CACHE, _GUIREN_CACHE
    _KE_JING_CACHE = None
    _GUIREN_CACHE = {}
    print("🗑️  缓存已清理")
    return {'success': True, 'message': '缓存已清理'}
```

然后调用：
```bash
curl -X POST http://localhost:5000/api/cache/clear
```

## ⚠️ 常见问题

### Q1: 优化后还是很慢（> 3 分钟）

**可能原因**:
1. 缓存未生效（检查是否重启服务器）
2. 服务器性能过低
3. 日期范围过大

**解决方法**:
- 确认已重启服务器
- 查看服务器日志中的性能统计
- 缩小日期范围测试（7 天）

### Q2: 内存占用过高

**可能原因**:
- 缓存数据过多

**解决方法**:
- 重启服务器清理缓存
- 添加缓存大小限制
- 定期清理缓存

### Q3: 结果不正确

**可能原因**:
- 缓存数据错误

**解决方法**:
- 清理缓存后重试
- 检查课经文件是否正确

## 📝 测试记录

### 测试 1: 7 天范围

- **日期**: 2026-11-01 至 2026-11-07
- **优化前**: 1-2 分钟
- **优化后**: ______ 秒
- **提升**: ______%

### 测试 2: 30 天范围

- **日期**: 2026-11-01 至 2026-11-30
- **优化前**: 5-10 分钟
- **优化后**: ______ 秒
- **提升**: ______%

### 测试 3: 60 天范围

- **日期**: 2026-11-01 至 2026-12-30
- **优化前**: 10-20 分钟
- **优化后**: ______ 秒
- **提升**: ______%

## 🎉 成功标志

当你看到以下日志时，说明优化成功：

```
✅ 完整日期范围分析完成：耗时 60-90 秒
📊 统计：共 30 天 × 12 时辰 = 360 次计算，找到 XXX 个候选
```

**性能提升**: 约 75-80% ✨

## 📞 技术支持

如遇到问题，请提供：
1. 服务器日志中的性能统计
2. 测试的日期范围
3. 浏览器控制台错误信息

---

**更新时间**: 2026-03-29  
**状态**: ✅ 已完成，等待验证  
**预期效果**: 性能提升 75-80%
