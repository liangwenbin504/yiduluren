# 500 错误问题诊断报告

## 📊 问题现状

**时间**: 2026-03-30 05:30  
**状态**: 🔴 所有 API 返回 500 错误  

### 症状

1. ✅ 服务器正常启动
2. ✅ 端口 5000 正常监听
3. ✅ 模块导入成功
4. ✅ 功能测试正常（直接调用 Python 函数）
5. ❌ **所有 HTTP 请求返回 500 错误**
6. ❌ **Flask 日志没有显示任何请求记录**

### 测试结果

| 测试类型 | 结果 | 说明 |
|---------|------|------|
| 模块导入 | ✅ 成功 | 所有大六壬模块正常导入 |
| 功能测试 | ✅ 成功 | `arrange_tiandi_pan`, `qi_sike`, `fa_sanchuan` 正常 |
| HTTP 请求 | ❌ 失败 | 所有请求返回 500 |
| 服务器日志 | ❌ 无记录 | 没有显示任何请求 |

## 🔍 已尝试的解决方案

### 1. 修复导入路径 ✅
```python
paths_to_add = [
    os.path.join(os.path.dirname(__file__), 'core_modules', 'engine'),
    os.path.join(os.path.dirname(__file__), 'src', 'utils'),
    os.path.join(os.path.dirname(__file__), 'zongmen', 'src', 'utils'),
    os.path.join(os.path.dirname(__file__), 'zongmen', 'src', 'engine')
]
```

### 2. 启用详细日志 ✅
```python
logging.basicConfig(level=logging.DEBUG)
app.run(debug=False, threaded=True, passthrough_errors=True)
```

### 3. 清理端口占用 ✅
```bash
taskkill /F /PID 18688  # 停止占用端口的进程
```

## ❓ 可能的根本原因

### 假设 1: Flask 请求处理链在某处失败
- **证据**: 日志没有显示请求
- **可能性**: 高
- **验证方法**: 添加 WSGI 中间件记录所有请求

### 假设 2: 端口冲突或绑定问题
- **证据**: 发现两个进程监听 5000 端口
- **可能性**: 中
- **验证方法**: 使用不同端口测试

### 假设 3: Flask 配置问题
- **证据**: `threaded=True` 可能导致日志不输出
- **可能性**: 中
- **验证方法**: 尝试 `threaded=False`

### 假设 4: 请求被中间件拦截
- **证据**: 限流器或缓存可能拦截了所有请求
- **可能性**: 高
- **验证方法**: 临时禁用限流和缓存

## 🔧 建议的下一步

### 方案 1: 使用 Gunicorn（推荐）⭐

```bash
# 安装 Gunicorn
pip install gunicorn

# 使用 Gunicorn 启动
gunicorn -w 4 -b 0.0.0.0:5000 --access-logfile access.log --error-logfile error.log --log-level debug api_server:app
```

**优点**:
- 生产级 WSGI 服务器
- 详细的日志输出
- 更好的错误处理
- 支持并发

### 方案 2: 临时启用调试模式

```python
# api_server.py
app.run(host='0.0.0.0', port=5000, debug=True, threaded=False)
```

**优点**:
- Flask 会显示详细错误堆栈
- 自动重载便于调试

**缺点**:
- 不适合生产环境
- 性能较差

### 方案 3: 添加 WSGI 中间件

```python
# 在 api_server.py 中添加
class LoggingMiddleware:
    def __init__(self, app):
        self.app = app
        
    def __call__(self, environ, start_response):
        print(f"收到请求：{environ.get('PATH_INFO')}")
        try:
            return self.app(environ, start_response)
        except Exception as e:
            print(f"请求失败：{e}")
            raise

app = LoggingMiddleware(app)
```

**优点**:
- 捕获所有请求
- 显示详细错误

### 方案 4: 禁用限流和缓存测试

```python
# 临时注释掉限流和缓存
# RATE_LIMIT_ENABLED = False
# async_task_handler 中的部分功能
```

**优点**:
- 排除中间件干扰
- 简化问题

## 📝 执行计划

### 立即执行（推荐）

1. **使用 Gunicorn 启动**
   ```bash
   pip install gunicorn
   gunicorn -w 2 -b 0.0.0.0:5000 --log-level debug api_server:app
   ```

2. **测试 API**
   ```bash
   python simple_api_test.py
   ```

3. **查看 Gunicorn 日志**
   - access.log - 请求日志
   - error.log - 错误日志

### 备用方案

如果 Gunicorn 不可用：
1. 临时启用 Flask 调试模式
2. 添加 WSGI 中间件
3. 逐个禁用中间件排查

## 📚 相关文档

- [Flask 调试指南](https://flask.palletsprojects.com/en/2.0.x/debugging/)
- [Gunicorn 文档](https://docs.gunicorn.org/en/stable/)
- [WSGI 中间件](https://flask.palletsprojects.com/en/2.0.x/advancedforeword/#middleware)

---

**报告生成时间**: 2026-03-30  
**问题状态**: 🔴 待解决  
**优先级**: 🔴 高  
**建议方案**: 使用 Gunicorn
